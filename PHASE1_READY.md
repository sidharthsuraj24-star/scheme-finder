# Phase 1 ready

Seed data is in `data/` and `docs/`. Build FastAPI + Next.js per docs/API_CONTRACT.md and docs/DECISIONS.md.

- Deterministic match first; LLM/template explanations only
- SQLite if no Supabase
- EN + ML UI
- >=8 unit tests from sample_profiles.json
