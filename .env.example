# Backend (copy to .env — never commit real secrets)
ENV=development
CORS_ORIGINS=*
# SCHEMES_PATH=  # optional; must stay under repo /app data roots
# LLM_API_KEY=
# RATE_LIMIT_MAX=30
# RATE_LIMIT_WINDOW_SEC=60
# MAX_BODY_BYTES=65536
